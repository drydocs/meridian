// TODO(#issue-3): replace placeholder with live data from useVaults hook
export function VaultList() {
  return (
    <section>
      <h2 className="text-lg font-semibold mb-4">Available Vaults</h2>
      <p className="text-gray-400 text-sm">Loading vaults…</p>
    </section>
  );
}
